{
  "name": "hazard-reporting-app",
  "version": "1.0.0",
  "description": "Full-stack hazard reporting app for India without database",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "install-all": "npm install && cd server && npm install"
  },
  "keywords": ["hazard", "reporting", "india", "weather", "emergency"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^4.9.5"
  }
}
